# Migration `watch-20190820123433`

This migration has been generated by Enoch at 8/20/2019, 12:34:38 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE `prisma2`.`User`(`id` int NOT NULL DEFAULT 0 ,`name` varchar(767) NOT NULL DEFAULT '' ,PRIMARY KEY (`id`));
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..watch-20190820123433
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,18 @@
+datasource mysql {
+  provider = "mysql"
+  url      = "mysql://admin:password@database-1.cjfbolkidkuz.us-east-2.rds.amazonaws.com:3306/prisma2"
+  //url      = "mysql://root:prisma@localhost:3306/db"
+}
+
+generator photonjs {
+  provider = "photonjs"
+}
+
+generator nexus_prisma {
+  provider = "nexus-prisma"
+}
+
+model User {
+  id   Int    @id
+  name String
+}
```

## Photon Usage

You can use a specific Photon built for this migration (watch-20190820123433)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/watch-20190820123433'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
