# Migration `20190815141823`

This migration has been generated by Enoch at 8/15/2019, 2:18:23 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE `localhost:3306`.`User`(`id` int NOT NULL DEFAULT 0 ,`name` varchar(767) NOT NULL DEFAULT '' ,PRIMARY KEY (`id`));
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..20190815141823
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,9 @@
+datasource mysql {
+  provider = "mysql"
+  url      = "http://localhost:3306"
+}
+
+model User {
+  id   Int    @id
+  name String
+}
```

## Photon Usage

You can use a specific Photon built for this migration (20190815141823)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190815141823'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
